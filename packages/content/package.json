{
  "name": "@repo/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm --filter @repo/utils build && tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "pnpm --filter @repo/utils build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@repo/utils": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vitest": "^4.0.15"
  }
}
